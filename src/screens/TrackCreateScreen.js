import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrckCreateScreen = () => {
    return <Text style={{ fontSize: 48 }}>TrckCreateScreen</Text>
}

const styles = StyleSheet.create({});

export default TrckCreateScreen;